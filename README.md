# Arsen Latipov Profile

Flutter Web сайт-визитка с фото, контактами, публичными coding-профилями и
карточками проектов.

## Run

```sh
flutter pub get
flutter run -d web-server --web-hostname 127.0.0.1 --web-port 55123
```

## Check

```sh
dart format lib test
flutter analyze
flutter test
```
